AGENTS.md

You are taking over a partially completed Angular frontend.

Goal: finish the existing application completely. Do not rebuild or replace working code.

Workflow

Inspect → Plan → Implement → Run → Fix → Continue

First inspect the existing codebase, routes, components, services, models, APIs, state, and styling. Identify what is complete, incomplete, broken, or missing.

Then implement the remaining work autonomously. Do not wait for instructions for obvious next steps.

Rules
Preserve existing architecture and working functionality.
Follow existing Angular patterns and UI conventions.
Reuse existing components/services where possible.
Avoid unnecessary dependencies or rewrites.
Keep data/services separate from presentation.
Use existing APIs; use centralized mock data only where APIs are unavailable.
No placeholder UI or unnecessary hardcoded data.
Make interactions functional, not just visual.
Handle loading, empty, error, and processing states.
Ensure responsive behavior.
Fix issues you encounter rather than only reporting them.
Priority
Broken/core functionality
Missing and incomplete screens
User flows/navigation
Data and interactions
States/responsiveness
Visual polish
Validation

Regularly run the Angular build and fix TypeScript, template, runtime, and console errors.

Test major routes and flows before considering the work complete.

Do not stop after completing one feature. Continue until the application is genuinely finished and production-ready.